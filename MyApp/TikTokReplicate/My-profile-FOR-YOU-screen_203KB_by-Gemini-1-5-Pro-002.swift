//
//  My-profile-FOR-YOU-screen_203KB_by-Gemini-1-5-Pro-002.swift
//  MyApp
//
//  Created by Cong Le on 11/16/24.
//


/// The UI elements in the image are not aligned correct (but acceptable).
/// The UI elements have color coded.
/// Buttons are clickable but not funtional with print statement.
/// In this case, the image size is 203 KB, dimention is 1284 × 2778, resolution is 72 × 72
/// Code generated by Gemini 1.5 Pro 002
import SwiftUI
struct TikTokCameraView: View {
    var body: some View {
        ZStack {
            // Placeholder for Camera Feed
            Rectangle()
                .fill(.black) // Or use a live camera feed here
            
            VStack {
                // Top Navigation Bar
                HStack {
                    Button(action: {}) {
                        Image(systemName: "tv.fill") // "LIVE" button
                            .font(.system(size: 17, weight: .semibold))

                    }
                        .foregroundColor(.red)
                        .padding(.leading, 10)
                    
                    Text("Explore")
                    Text("Following")
                        .foregroundColor(.red)

                    
                    Text("Shop")
                    Text("For You")
                        .underline()
                    Spacer()
                    Image(systemName: "magnifyingglass")
                }
                .padding(EdgeInsets(top: 40, leading: 10, bottom: 0, trailing: 10))
                .font(.system(size:15, design: .default))
                .foregroundColor(.white)




                Spacer()
                
                // Bottom Bar
                HStack {
                    VStack {
                        Button(action: {}){
                            Image(systemName: "house.fill")
                        }
                        Text("Home")
                            .font(.caption)

                    }
                    .padding(.horizontal, 1)

                    

                    VStack{
                        HStack {
                            Image(systemName: "person.2.fill")
                            Circle()
                                .fill(Color.red)
                                .frame(width: 12, height: 12)
                                .overlay(
                                    Text("1")
                                        .foregroundColor(.white)
                                        .font(.caption2)
                                        .offset(x:-1, y:0)

                                )
                                .offset(x: 10, y:-7)
                        }
                        Text("Friends")
                            .font(.caption)

                    }
                    .padding(.horizontal, 1)

                    // Center Button (Large Plus)
                    ZStack {
                        Circle()
                            .fill(.white)
                            .opacity(0.6)
                            .frame(width: 60, height: 60)
                            .overlay(
                                    Circle()
                                    .fill(.pink)
                                    .frame(width: 50, height: 50)

                                .overlay(
                                    Image(systemName: "plus")
                                        .resizable()
                                        .scaledToFit()
                                        .scaleEffect(0.5)

                                        .foregroundColor(.white)


                                )
                            )
                    }

                    .padding(1)
                    
                    VStack {

                        Image(systemName: "envelope.fill")

                        Text("Inbox")
                            .font(.caption)
                    }
                    .padding(.horizontal, 1)


                    VStack{
                        Image(systemName: "person")

                        Text("Profile")
                            .font(.caption)

                    }
                    .padding(.horizontal, 1)


                }
                    .frame(maxWidth: .infinity)
                    .padding()
                    .font(.system(size: 13, weight: .semibold))
                    .foregroundColor(.white)
                    .background(
                        VStack{
                            Spacer()
                            Capsule()
                                .fill(.gray)
                                .frame(height:3)
                                .padding(.bottom, 10)
                        }
                    )




            }
            VStack {
                    Spacer()


                    //Right Side Elements of TikTok
                    HStack {
                        Spacer()
                        VStack(spacing: 20) {
                            // Profile picture with plus button
                            ZStack(alignment: .bottomTrailing) {
                                CircleImage(imageName: "profilePicture") // Replace with your image name/URL
                                    .frame(width: 50, height: 50)
                                Circle()
                                    .fill(.white)
                                    .frame(width: 20, height: 20)
                                    .overlay(
                                        Image(systemName: "plus")
                                            .foregroundColor(.black)
                                    )
                            }

                            Button(action: {}) {
                                Image(systemName: "heart.fill")

                            }
                                .foregroundColor(.white)
                                .font(.system(size:30))
                                .overlay(
                                    Text("69.4K")
                                        .font(.caption)
                                        .foregroundColor(.white)
                                        .offset(x:20, y:20)

                                )
                            Button(action: {}) {
                                Image(systemName: "message.fill")
                                    .font(.system(size:26))

                            }
                                .foregroundColor(.white)
                                .overlay(
                                    Text("213")
                                        .font(.caption)
                                        .foregroundColor(.white)
                                        .offset(x:20, y:20)
                                        
                                )
                            Button(action: {}) {
                                Image(systemName: "bookmark.fill")
                                    .font(.system(size:26))

                            }
                                .foregroundColor(.white)
                                .overlay(
                                    Text("6,563")
                                        .font(.caption)
                                        .foregroundColor(.white)
                                        .offset(x:20, y:20)
                                )
                            Button(action: {}) {
                                Image(systemName: "arrowshape.turn.up.right.fill")
                                    .font(.system(size:26))

                            }
                                .foregroundColor(.white)
                                .overlay(
                                    Text("5,094")
                                        .font(.caption)
                                        .foregroundColor(.white)
                                        .offset(x:20, y:20)
                                )



                        }

                        .padding()
                    }


                VStack(alignment:.leading){
                    Spacer()
                    Text("Phan Ái Vy")
                        .font(.headline)
                        .foregroundColor(.white)

                    Text("Z ha #phanaizi #fyp #viral")
                        .font(.callout)
                        .foregroundColor(.white)

                    Text("See translation")
                        .foregroundColor(.gray)
                        .font(.caption)




                }
                .padding()

            }



        }
        .edgesIgnoringSafeArea(.all) // Important for full-screen experience

    }
}





struct CircleImage: View {
    let imageName: String
    var body: some View {
        Image(imageName)
            .resizable()
            .scaledToFill()
            .clipShape(Circle())
    }
}




struct CameraInterface_Previews: PreviewProvider {
    static var previews: some View {
        TikTokCameraView()
    }
}
