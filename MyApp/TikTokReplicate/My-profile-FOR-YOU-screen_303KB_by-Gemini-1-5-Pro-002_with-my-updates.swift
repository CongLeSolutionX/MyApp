//
//  My-profile-FOR-YOU-screen_303KB_by-Gemini-1-5-Pro-002.swift
//  MyApp
//
//  Created by Cong Le on 11/16/24.
//

/// Evaluation on the code quality . TBD
/// In this case, the image size is 303 KB, dimention is 1284 × 2778, resolution is 72 × 72
/// Code generated by Gemini 1.5 Pro 002
import SwiftUI
struct TikTokLikeInterface_withMyUpdates: View {
    var body: some View {
        ZStack {
            // Placeholder for Video Content
            Rectangle()
                .fill(Color.gray) // Replace with actual video feed
            
            VStack {
                // Top Bar
                HStack {
                    Image(systemName: "tv.fill")
                        .foregroundColor(.red)
                    Text("Explore")
                    Text("Following")
                        .foregroundColor(.pink)
                    Text("Shop")
                        .foregroundColor(.pink)
                    Text("For You")
                        .font(.headline) // or appropriate styling
                    Spacer()
                    Image(systemName: "magnifyingglass")
                }.padding(.horizontal)
                    .padding(.top, 50) // Adjust for safe area
                
                Spacer()
                
                
                // Bottom Section
                VStack(spacing: 0) {
                    // Caption and Interactions
                    VStack(alignment: .leading, spacing: 4) {
                        Text("Phan Ái Vy")
                            .font(.headline)
                        Text("Z ha #phanaizi #fyp #viral")
                            .font(.caption)
                        Text("See translation")
                            .font(.caption2)
                            .foregroundColor(.gray)
                        
                    }.padding()
                        .frame(maxWidth: .infinity, alignment: .leading)
                    
                    
                    // Bottom Tab Bar
                    HStack {
                        VStack {
                            Image(systemName: "house.fill")
                            Text("Home")
                        }
                        Spacer()
                        VStack {
                            Image(systemName: "person.2.fill")
                            Text("Friends")
                        }.overlay(
                            Circle()
                                .fill(Color.red)
                                .frame(width: 16, height: 16)
                                .overlay(Text("1").font(.caption2).foregroundColor(.white))
                                .offset(x: 10, y: -10)
                        )
                        Spacer()
                        
                        ZStack {
                            Circle()
                                .fill(Color.pink)
                                .frame(width: 60, height: 60)
                            Image(systemName: "plus")
                                .font(.largeTitle)
                                .foregroundColor(.white)
                        }
                        
                        
                        Spacer()
                        VStack {
                            Image(systemName: "bubble.left.and.bubble.right.fill")
                            Text("Inbox")
                        }
                        Spacer()
                        VStack {
                            Image(systemName: "person.fill")
                            Text("Profile")
                        }
                    }
                    .padding()
                    .frame(maxWidth: .infinity)
                    .background(.black)
                }
                
                
                
                // Right Side Elements
                VStack {
                    HStack {
                        Spacer() // Push elements to the right
                        VStack {
                            CircleImage(imageName: "profile_image") // Replace with avatar URL
                                .frame(width: 50, height: 50)
                            Circle()
                                .fill(Color.white.opacity(0.8))
                                .frame(width: 20, height: 20)
                                .overlay(
                                    Image(systemName: "plus")
                                        .font(.system(size: 15))
                                        .foregroundColor(.pink)
                                )
                                .offset(x: 10,y:-10)
                        }.padding(20) // Adjust padding if needed
                        
                        Spacer()
                        
                        
                        VStack(spacing: 24) {
                            Image(systemName: "heart.fill")
                            Text("69.4K")
                            Image(systemName: "bubble.left.fill")
                            Text("213")
                            Image(systemName: "bookmark.fill")
                            Text("6,563")
                            Image(systemName: "arrowshape.turn.up.right.fill")
                            Text("5,094")
                            
                            CircleImage(imageName: "profile_image_small") // Replace with avatar
                                .frame(width: 30, height: 30)
                        }.padding(20)
                            .foregroundColor(.white)
                        
                        
                        
                    }.frame(maxWidth: .infinity, alignment: .trailing)
                }
            }
            .edgesIgnoringSafeArea(.all)
        }
    }
    
    // Helper view for circular images (replace with your image loading logic)
    struct CircleImage: View {
        let imageName: String // Placeholder - replace with URL or Image variable
        var body: some View {
            Image(imageName) // Replace with Image loading from your source
                .resizable()
                .scaledToFill()
                .clipShape(Circle())
        }
    }
}

struct TikTokLikeInterface_withMyUpdates_Previews: PreviewProvider {
    static var previews: some View {
        TikTokLikeInterface()
    }
}
